'use strict';

export const CAREER_FIELDS = Object.freeze([
  'currentTitle',
  'targetTitle',
  'currentSkills',
]);

export const RESUME_FIELDS = Object.freeze([
  'resumeStoragePath',
  'fileName',
  'mimeType',
]);

export const SALARY_FIELDS = Object.freeze([
  'jobTitle',
  'location',
  'yearsExperience',
  'industry',
]);