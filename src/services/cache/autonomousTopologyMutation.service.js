'use strict';

/**
 * autonomousTopologyMutation.service.js — STUB
 *
 * Missing file that caused server crash.
 *
 * API surface used by server.js:
 *   - startMutationWorker()  → returns a handle with .shutdown()
 */

function startMutationWorker() {
  return {
    shutdown() {
      // no-op stub
    },
  };
}

module.exports = {
  startMutationWorker,
};
