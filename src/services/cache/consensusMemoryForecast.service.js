'use strict';

/**
 * consensusMemoryForecast.service.js — STUB
 *
 * Missing file that caused server crash.
 *
 * API surface used by server.js:
 *   - startForecastLoop()  → returns a handle with .shutdown()
 */

function startForecastLoop() {
  return {
    shutdown() {
      // no-op stub
    },
  };
}

module.exports = {
  startForecastLoop,
};
