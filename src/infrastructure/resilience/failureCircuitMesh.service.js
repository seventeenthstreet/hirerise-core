'use strict';

/**
 * failureCircuitMesh.service.js — STUB
 *
 * Missing file — would crash server at startup (line 5136).
 *
 * API surface used by server.js:
 *   - getAllCircuitStates()  → returns an object (JSON-serialized in logs)
 */

function getAllCircuitStates() {
  return {};
}

module.exports = {
  getAllCircuitStates,
};
